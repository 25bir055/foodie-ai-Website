const fs = require('fs'); const { OpenAI } = require('openai'); require('dotenv').config(); const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY }); async function t() { try { fs.writeFileSync('test.webm', 'dummy'); const res = await openai.audio.transcriptions.create({ file: fs.createReadStream('test.webm'), model: 'whisper-1' }); console.log(res.text); } catch (e) { console.error('ERROR:', e.message); } } t();
