const fs = require('fs'); const axios = require('axios'); const FormData = require('form-data'); async function t() { try { const form = new FormData(); fs.writeFileSync('dummy.webm', 'RIFF....WEBM...'); form.append('audio', fs.createReadStream('dummy.webm')); const res = await axios.post('http://localhost:5000/api/chat/audio', form, { headers: form.getHeaders() }); console.log(res.data); } catch(e) { console.log(e.response ? e.response.data : e.message); } } t();
